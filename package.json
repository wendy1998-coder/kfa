{
  "name": "kfa",
  "version": "1.0.0",
  "main": "functions/server.js",
  "scripts": {
    "build": "cd kaw-furniture-assembler && npm install && ng build --prod && cd ../ && netlify-lambda build express",
    "start": "nodemon server.js",
    "start:lambda": "netlify-lambda serve express"
  },
  "dependencies": {
    "@angular/cli": "~10.0.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "netlify-lambda": "^2.0.16",
    "nodemon": "^2.0.4",
    "serverless-http": "^2.5.0"
  }
}
